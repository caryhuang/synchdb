apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    uid: aepv5908xcwe8f    # <-- matches template's datasource uid
    access: proxy
    orgId: 1
    url: http://prometheus:9090
    isDefault: true
    editable: true

